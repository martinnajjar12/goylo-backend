services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: goylo
    ports:
      - "5433:5432"
    volumes:
      - goylo_postgres:/var/lib/postgresql/data
volumes:
  goylo_postgres:
